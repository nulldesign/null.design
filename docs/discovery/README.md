# Discovery (private)

The Deliverable 01 discovery report for RUN-2026-0001 and its screenshots are **not committed to this repository**: they contain employer identifiers, a domain-expiry date, and notes on private infrastructure that the studio's own leakage rules (`docs/architecture/publishing-pipeline.md`) forbid on any public surface.

Canonical private copy: the studio's git-synced knowledge vault, `memory/projects/null-design/`.

What is public from that work lives in `/registry` (records), `/content` (case studies) and `registry/runs/RUN-2026-0001.yaml` (the run record, with critiques and pending decisions).
